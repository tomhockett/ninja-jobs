<% content_for(:page_title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @users, renderer: @custom_renderer %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users, renderer: @custom_renderer %>